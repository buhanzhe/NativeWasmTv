# 广西电视 ARM32 三包对比

2026-09-10，SM-G3608，Android 4.4.4 / API19，ADB 8ba8ceb8。

真实广西综艺旅游加密 TS 分片，1,502,308 字节，EXTINF 4 秒。线上库来自当前 TvWasm/cjs 站点清单 v1，下载后 SHA256 校验通过；另两份为本地 r17c/r25c 构建。

同一 app_process Java 基准、同一输入与解密参数，仅更换 SO。先按线上→基础→性能，再按性能→基础→线上运行，各包两个独立进程，每进程11次。剔除每进程首次调用，汇总20次。计时前读取并复制输入；哈希、写出和100ms间隔不计时。应用已停止，无网络下载计入解密时间。

| 包 | 平均 ms | 中位 ms | 范围 ms | 调用线程CPU平均 ms | 首次调用 ms |
|---|---:|---:|---|---:|---|
| online | 480.6 | 481.0 | 461–486 | 477.55 | [463, 457] |
| base | 579.4 | 579.5 | 573–585 | 577.25 | [560, 558] |
| perf | 216.5 | 217.0 | 210–223 | 212.9 | [195, 194] |

r25c 比线上减少54.95%耗时（约2.22倍吞吐），比r17c减少62.63%；r17c比线上增加20.56%。在此设备与此分片上，广西的r25c包明显更快。结论仅针对分片解密，不代表整条播放链路或所有设备。

全部66次结果相同，变化字节数1,377,424；输出SHA256：`354ffb0a817d8cfbd275381fc37680a6b77c5145f0838f7fcb790bdef3f52666`。电脑FFmpeg完整解码输出，返回0，无错误日志；不是电视端画面声音验收。

库加载为0–1ms，毫秒级测量且可能命中文件页缓存，不能当作完全冷启动速度。

输入SHA256：`05429cd4f2d92d68722bae4e19fdd62b12cd3121ef77906ee80642dcc90fc174`。原始URL和参数保存在本地 fixture.json，清单保存在 online-manifest.json。

- online: 17808 字节；SHA256 `a4ede8823ca91a282af61c879cdf91416a4facd386d573b67ba6437c218d5e0e`。
- base: 18028 字节；SHA256 `f2f5d7de5f4eb64baf1a794dde11eecc81774ddb707241b028fc3c159815c932`。
- perf: 8000 字节；SHA256 `f2d4c76261cfc0412e4f360cc85996d96278808bfbb55a66c413454415c890db`。

日志、基准源码与统计：`.codex-tmp/gxtv-profile-compare/`。不覆盖APK，不修改频道配置，测试后应用保持退出。
